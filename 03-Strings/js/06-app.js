const producto = 'Monitor1 20 pulgadas';

// repeat 
// repite un texto
const texto = 'en promo!'.repeat(3);
console.log(texto);

console.log(`${producto} ${texto}`);

// split
// divide un string
const actividad = 'Estoy aprendiendo JavaScript';
console.log(actividad.split(" "));

const hobbies = 'programar, dibujar, tocar guitarra, ver anime'
console.log(hobbies.split(", "));