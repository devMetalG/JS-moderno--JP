function sumar(a, b){ // a y b son parametros de función
  console.log(a + b);
};

sumar(2, 3); // 2 y 3 son argumentos de función
sumar(200, 321); 

function saludar(nombre, apellido) {
  console.log(`Hola, ${nombre} ${apellido}`)
};

saludar('Memo', 'Franco');
saludar('Memo');
saludar();